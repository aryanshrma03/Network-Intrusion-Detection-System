# Data

This directory is reserved for network-flow datasets.

The application accepts CSV flow metadata with:

```text
timestamp,src_ip,dst_ip,src_port,dst_port,protocol,flags,bytes
```

Do not commit sensitive production traffic logs to the repository.
